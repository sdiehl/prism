-- Runtime fixture embedded by the wasm continuation-teleport demo.

fn count_up(i, last) =
  if i > last then
    ()
  else
    println("step {i}: {i} squared is {i * i}")
    count_up(i + 1, last)

fn main() = count_up(1, 10)
