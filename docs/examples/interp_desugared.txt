-- A string with holes becomes a concatenation of its literal pieces and the
-- type-directed display of each hole. Display renders a top-level string raw
-- (unquoted), unlike the `show` method, which quotes strings.
"hello {name}, n = {n}"
  ==>
concat("hello ", concat(display(name), concat(", n = ", display(n))))
