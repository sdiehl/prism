/target
/target-mlir
/tests/projects/*/target/
/tools/prismup/target/
/docs/book
/web/pkg
/web/src/examples.ts
# The code indexes the viewer page loads, including the revision pairs `just
# index-diff` writes: generated artifacts, regenerated from source like the wasm
# bundle rather than committed.
/web/public/*-index.json
# Transpiled output of the viewer checks (`pnpm check:viewer`).
/web/test/build
/web/node_modules
/web/dist
.DS_Store
*.ll
*.mlir
*.bc
*.o
*.prism_rt.c
/a.out
/models/.lake
/models/fixtures/*.json
/models/semantics/*.pdf
/docs/src/semantics.pdf
/docs/src/pkg
/public
/result
/result-*
/dist
/.direnv
# Generated outputs of the lineage_tiles example (the run writes these); the
# inputs (pipeline.pr, config.json, tiles/) are committed, the artifacts are not.
/examples/lineage_tiles/summary.json
/examples/lineage_tiles/*.replay
/examples/lineage_tiles/*.plineage
/examples/spectra-deck/target/
# Local private-workflow helper scripts: kept on disk, never shipped (they drive
# the private scratch remote, not the compiler).
/block_public
/unblock_public
/push_private
/pull_private
setup.sh
__pycache__
# Internal parser-experiment working notes: kept on disk, never shipped.
# The scoreboard itself is committed: scripts/scoreboard.py --check pins it in CI.
/docs/internal/PARSER_COMPACTION_BASELINE.md
/docs/internal/PARSER_COMPACTION_CORPUS.md
/docs/internal/PARSER_GENERATOR_SEMANTICS.md
/docs/internal/PARSER_PERFORMANCE_PROTOCOL.md
/docs/internal/PARSER_PRODUCTION_MANIFEST.md
