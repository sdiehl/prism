effect State
  get() : Int
  put(Int) : Unit

fn prog() : Int ! {State} =
  let x = get()
  put(x + 1)
  let y = get()
  y * 10

fn main() : Int =
  let f =
    handle prog() with
      get() resume k => \(s) -> k(s)(s)
      put(n) resume k => \(_s) -> k(())(n)
      return r => \(_s) -> r
  f(0)
