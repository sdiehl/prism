# Auto-load the nix dev shell (LLVM_SYS_221_PREFIX, PRISM_CC, PATH, rust-analyzer)
# on cd, for the terminal and the editor. Run `direnv allow` once. Install
# nix-direnv for cached reloads: https://github.com/nix-community/nix-direnv
use flake
