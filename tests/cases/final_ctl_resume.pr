-- A never clause declares it never resumes; a free `resume` in its body
-- is rejected at desugar with the span of the use.

effect Abort
  abort(Int) : Int

fn main() =
  let v =
    handle abort(7) with
      never abort(code) => resume(code)
      return r => r
  println(v)
