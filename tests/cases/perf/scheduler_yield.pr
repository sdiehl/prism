import Concurrent (..)

fn spin(k : Int) : Int ! {Async(Int)} =
  if k == 0 then
    0
  else
    yield(())
    spin(k - 1)

fn app() : Int ! {Async(Int)} = spin(__N__)

fn main() = println(run_strategy(app))
