-- Usage facts other than `noalloc` parse but are reserved: the checker
-- rejects them by name until their checkers exist, so no program can give a
-- reserved fact a meaning.
fn apply(f : ((Int) -> Int) @ {once, portable}, x : Int) : Int = f(x)

fn main() : Int = apply(\(y) -> y + 1, 41)
