-- A `once` operation (grade Once) resumes exactly once. A bare clause that
-- resumes `poll` twice exceeds the declared grade and is rejected.
effect Poll
  once poll() : Int

fn use_it() : Int ! {Poll} = poll()

fn main() =
  handle use_it() with
    poll() resume k => k(1) + k(2)
    return r => r
