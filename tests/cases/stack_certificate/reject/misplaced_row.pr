-- The claim certifies a declaration, not a parameter type: a `@ bounded_stack`
-- row anywhere but the root of a `fn` return annotation is misplaced, and the
-- diagnostic names the row that was written.
fn f(x : Int @ bounded_stack) : Int = x
