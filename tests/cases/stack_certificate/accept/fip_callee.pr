-- A `fip` callee is a stack certificate: its own check already proves it
-- runs in bounded stack, so a claimed caller may rely on it.
fip fn id(x) = x

fn top(x : Int) : Int @ bounded_stack = id(x)
