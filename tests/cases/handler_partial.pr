-- An unmarked handler still promises exhaustive coverage. Omission is accepted
-- only by the explicit `with partial` form, whose effect remains residual.
effect E
  one() : Int
  two() : Int

fn main() =
  println(handle one() + two() with {
    one() resume k => k(1),
    return r => r
  })
