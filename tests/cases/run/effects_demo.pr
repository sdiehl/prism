fn safe_div(a, b) =
  if b == 0 then
    Err(0)
  else
    Ok(a / b)

fn chain(x) =
  and_then_result(\(a) -> and_then_result(\(b) -> Ok(a + b), safe_div(x, 2)), safe_div(100, x))

fn main() =
  println(result_or(0 - 1, chain(5)))
  println(result_or(0 - 1, chain(0)))
