// The gallery is a static landing page; this module exists only to pull the
// gallery stylesheet (and, through it, the shared theme) into the bundle, the
// same way the other pages import their CSS. No runtime behaviour.
import "./gallery.css";
